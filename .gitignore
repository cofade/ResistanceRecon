# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.python-version

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/

# ML / experiment tracking
mlruns/
*.joblib
*.pkl

# Data & model artifacts (large; published as release assets / reproducible via scripts)
data/raw/
data/interim/
data/processed/
# models/: commit the small TEXT artifacts (feature_schema/metrics/conformal/coefficients JSON,
# model_card.md, registry.json, results_summary.json) as reproducible ground-truth, AND the tiny
# per-drug calibrated_model.joblib (~3KB each) -- the EPIC 6 demo/API/UI and CI must load the
# registry from a bare clone (ADR-0022), so these five small demo models are committed. The
# release-asset convention still governs any FUTURE large model; MLflow runs (mlruns/) stay out.
models/*
!models/.gitkeep
!models/*/
!models/**/*.json
!models/**/*.md
!models/**/*.joblib

# Secrets
.env
.env.*
!.env.example
*.key

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/

# Keep small test fixtures tracked
!tests/fixtures/**

# MCP config can contain API keys — never commit
.claude/.mcp.json
.mcp.json

# Internal reuse map references private projects — keep local, do not publish
Documentation/reuse-inventory.md
